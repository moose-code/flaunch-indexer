/**
 * Flaunch Indexer Event Handlers
 *
 * This file imports all handlers from the modular handlers directory.
 * Each handler file corresponds to a contract or group of related contracts.
 */

import "./handlers";
